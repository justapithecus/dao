alias int8 as c_char
alias int32 as c_int

external(C) function puts(str: ptr c_char) -> c_int

function double(x)
  x * 2

if double(5) > 9 then
  puts("10 is greater than 9")
else
  puts("Unreachable")
